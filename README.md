# Spring Scala Jackson template

A template repository with "all" you need for Java-Scala interop in a Spring
Boot project.

Handles serialization and deserialization of `case class`es and `Option`
correctly.
